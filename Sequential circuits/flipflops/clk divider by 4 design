module clk_div4(
    input clk,
    input rst,
    output clk_out
);

reg [1:0] count;

always @(posedge clk or posedge rst)
begin
    if(rst)
        count <= 2'b00;
    else
        count <= count + 1;
end

assign clk_out = count[1];  

endmodule
